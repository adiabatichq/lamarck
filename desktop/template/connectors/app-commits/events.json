{
  "catalogVersion": 1,
  "eventTypes": {
    "app.commit": {
      "description": "A Git commit observed in the source repository of a Lamarck App.",
      "payloadSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "additionalProperties": false,
        "required": ["appId", "commitSha", "authorName", "authorEmail", "message"],
        "properties": {
          "appId": {
            "type": "string",
            "description": "Stable Lamarck App ID whose source repository contains the commit."
          },
          "commitSha": {
            "type": "string",
            "description": "Full Git object ID of the commit."
          },
          "authorName": {
            "type": "string",
            "description": "Author name recorded in the Git commit."
          },
          "authorEmail": {
            "type": "string",
            "description": "Author email recorded in the Git commit."
          },
          "message": {
            "type": "string",
            "description": "Complete Git commit message with trailing whitespace removed."
          }
        }
      }
    }
  }
}
